name: Sync Upstream

on:
  schedule:
    - cron: '0 3 * * *'   # 每日 UTC 03:00 (香港 11:00)
  workflow_dispatch:

permissions:
  contents: write

jobs:
  sync-upstream:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout fork
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Fetch upstream (jangviktor-web/likeskill)
        run: |
          git remote add upstream https://github.com/jangviktor-web/likeskill.git
          git fetch upstream

      - name: Fast-forward sync (master)
        run: |
          git checkout master
          if git merge --ff-only upstream/master; then
            git push origin master
            echo "Synced master to upstream/master"
          else
            echo "::warning::Not fast-forwardable (local commits?) - manual sync needed"
            exit 1
          fi
